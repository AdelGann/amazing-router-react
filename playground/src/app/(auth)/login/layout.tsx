import { Outlet } from "@amazing-router/react";

const LoginLayout = () => {
  return (
    <div>
      prueba de login layout - y subhijos
      <Outlet />
    </div>
  );
};

export default LoginLayout;
